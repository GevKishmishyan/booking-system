spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booking_system?createDatabaseIfNotExist=true&currentSchema=booking_system
    username: user
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true
    schemas: booking_system