spring:
  profiles:
    active: @spring.profiles.active@
  mail:
    host: smtp.gmail.com
    port: 587
    username: 2021JavaForTest@gmail.com
    password: forTest2021
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    confirmLinkIP : http://localhost:8080/

jwt:
  secret: bookingSystem
  # those are the expiration durations for jwt access and jwt refresh
  # introduced in milliseconds which are equal to 3 minutes and 5 minutes
  accessExpirationMs: 1800000
  refreshExpirationMs: 3000000

redis:
  # exchange these fields for yours
  host: localhost
  port: 6379
  password: 12345

path:
    uploadPicturePath: C:\Users\artur\Desktop\booking-system-uploaded-pictures