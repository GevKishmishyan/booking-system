package com.epam.bookingsystem.repository;

import com.epam.bookingsystem.model.RoomPictures;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomPicturesRepository extends JpaRepository<RoomPictures, Integer> {
}
